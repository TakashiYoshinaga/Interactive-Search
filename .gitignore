# Neo4j credentials — never commit
DoNotUpload/
# 旧綴り。取り違えたフォルダが残っていても守れるように残しておく
DoNotUpdate/
Neo4j-*-Created-*.txt
*.env
.env
.env.*

# Dev tooling
tools/node_modules/
node_modules/
package-lock.json

# OS / editor
.DS_Store
Thumbs.db
desktop.ini
*.swp

# 開発メモ（ローカル専用・公開しない）
PLAN.md

# 開発用の一時ハーネス（公開しない）
_dev/
